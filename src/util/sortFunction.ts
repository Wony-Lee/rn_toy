import {ISampleData} from '../components/DataTable'

export const numberWidthStringSort = (param: any) => {
  // param.sort(a, b) => numberWidthStringSort(b, a)
}

// (a: ISampleData, b: ISampleData) => {
//   console.log(a, b)
//   let orderKey: any = ''
//   if (a[orderKey] < b[orderKey]) return -1
//   if (a[orderKey] > b[orderKey]) return 1
//   if (a[orderKey] === b[orderKey]) return 0
//   else return -1
// }
